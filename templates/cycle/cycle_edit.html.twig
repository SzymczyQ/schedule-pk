{% extends 'base.html.twig' %}

{% form_theme cycleForm 'global/forms/config_horizontal_layout.html.twig' %}

{% block pageTitle %}
    {{ 'admin_edit_cycle.page_title'|trans }}
{% endblock %}

{% block pageContentDescriptionHeader %}
    {{ 'admin_edit_cycle.page_title'|trans }}
{% endblock %}

{% block pageContentDescriptionBreadcrumb %}{% endblock %}

{% block pageContent %}
    <div class="row">
        <div class="col-md-8 col-md-offset-2">
            <div class="box box-primary">
                <div class="box-header">
                    <h3 class="box-title">
                        {{ 'admin_edit_cycle.edit_box_header'|trans }}
                    </h3>
                </div>

                {{ form_start(cycleForm) }}
                    <div class="box-body">
                        {# FORM ERRORS #}
                        {{ form_errors(cycleForm) }}

                        {# INCLUDE FORM TOKEN #}
                        {{ form_row(cycleForm._token) }}

                        {{ form_row(cycleForm.name) }}
                        {{ form_row(cycleForm.faculty) }}

                        {# FORM SUBMIT #}
                        {{ form_row(cycleForm.submit,
                            {'label': 'edit_cycle_form.form_label.submit_edit', 'attr': {'class': 'btn-success'}}) }}
                    </div>
                {{ form_end(cycleForm, {'render_rest': false}) }}

            </div>
        </div>
    </div>
{% endblock %}